import { SafeAreaView, StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TownSquare = () => {
  return (
    <SafeAreaView>
      <Text>TownSquare</Text>
    </SafeAreaView>
  )
}

export default TownSquare

const styles = StyleSheet.create({})